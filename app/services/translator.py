def translate_text(text: str, target_lang: str) -> str:
    """
    Stub: aquí conectas tu proveedor (Azure Translator, DeepL, etc.).
    Por ahora, devuelve el mismo texto.
    """
    if not text:
        return ""
    return text
